# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'AuthTestAssignment' do
  use_frameworks!

  # Pods for AuthTestAssignment
  pod 'Swinject'
  pod 'Moya'
  pod 'IQKeyboardManager'

  target 'AuthTestAssignmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
